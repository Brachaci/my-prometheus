service:
  port: 8080
  type: ClusterIP

serviceAccount:
  enabled: true

ingress:
  enabled: false

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2

image:
  repository: https://qay.io/prometheus/alertmanager
  tag: latest
